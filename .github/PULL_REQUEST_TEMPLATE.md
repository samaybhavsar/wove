## What does this PR do?

<!-- A short summary of the change. -->

## Why?

<!-- Context, and any related issue: Fixes #123 -->

## Checklist

- [ ] Tested locally (`npx @wordpress/env start`)
- [ ] Design stays restrained — most styling lives in `theme.json`
- [ ] `php -l` passes and `theme.json` is valid
- [ ] No personal/demo content or hard-coded URLs introduced
- [ ] Updated `languages/wove.pot` if UI strings changed
